---
rules:
    - id: "zero_rule_test"
      description: "move from a to b"
      from: "file://tmp/origin/{client}/4.results/{file}"
      to: "s3://gjl-client-backup/{client}/{file}"
      glacier: true
      delete_after: true